# 파일럿 1단계 개시 전 최종 운영 점검 — 2026-09-16

> `.moai/docs/pilot-ops-launch-plan.md` §1~§2.2 기준. 이 문서는 실제로 수행한
> 점검 결과의 기록이며, 코드 변경은 포함하지 않는다(로그아웃 버튼 추가 및
> 색상 수정은 이 점검과 별개로 이미 각각 커밋 `f199c33`, `9050ff1`로 반영·
> 배포됨).

## 1. 배포 SHA 확인

- 요청 시 제시된 SHA(`4ef45bc...`, PR #12)는 **현재 main보다 3개 커밋 이전**이다.
- 실제 로컬/원격 main HEAD: **`9050ff1121...`**(`git fetch` 후 확인, local == origin, 워킹트리 clean).
  - `4ef45bc` → PR #12(문서화) → `ee21903` PR #13(datepicker) → `f199c33` 로그아웃 버튼 → `9050ff1` 색상 수정.
- **USER ACTION**: Netlify 대시보드 → Deploys 탭에서 최신 Published deploy의 커밋이 `9050ff1`인지 확인 필요 — 이 세션은 Netlify 대시보드에 접근할 수 없어 배포 완료 여부를 직접 확인하지 못했다.

## 2. 데이터 테스트 결과 (실측)

| 항목 | 결과 |
|---|---|
| 사용 계정 | `zuge3927@naver.com` (운영자 계정, 프로덕션) |
| 실행 사건 수 | 1건 (스모크런 ID `SMOKE-OPS1-20260916-01`) |
| Gemini 모델 요청 수 | 총 3건 — Premium(`gemini-3.6-flash`) 1건, Lite(`gemini-3.5-flash-lite`) 2건, 전부 `ok: true` |
| 202 → completed 전이 | PASS — `case_jobs.status`가 `completed`로 정상 전이 확인 |
| 사건·리포트 저장/재조회 | PASS — `cases.status: completed`, `reports` 1건 생성 확인 |
| 구조화 피드백 저장/재조회 | PASS — `feedback` 테이블에 실제 행 저장 확인 |
| 테넌트 격리(§2.3) | **미수행** — 1단계(운영자 단독)는 §2.1만 요구하며, §2.3은 2단계 진입 전에만 필요(문서 §3.1 명시) |
| 관측된 429/5xx (Netlify 함수 로그, §2.1 실패 이벤트 8종) | **미확인(Gap)** — 이 세션은 Netlify 함수 로그에 접근할 수 없다. 다만 DB상 사건·job이 예외 없이 `completed`로 정상 종료됐고, 재시도 흔적(중복 job 등)도 없었다 |

## 3. 스모크 데이터 정리(cleanup) — 재검증

세션 종료 직전 다시 읽기 전용으로 재확인(이 문서 작성 시점 기준):

| 테이블 | 대상 식별자 | 잔존 행 수 |
|---|---|---|
| `cases` | caseId `1bf3e871...` | 0 |
| `reports` | reportId `cf0a78da...` | 0 |
| `feedback` | caseId `1bf3e871...` | 0 |
| `case_jobs` | jobId `62a3c583...` | 0 |
| `gemini_request_observations` | jobId `62a3c583...` | 0 |
| `reservations` | ownerUserId(운영자) | 0 (활성 리스 없음) |

운영자 계정(`zuge3927@naver.com`) 자체는 실 파일럿 계정이므로 **삭제하지 않고 유지**했다 — 이번 정리는 스모크가 생성한 테스트 데이터에만 한정했고, 실제 파일럿 데이터는 애초에 존재하지 않아 건드릴 것이 없었다.

## 4. 운영자 계정 로그인 상태

- 비밀번호 재설정(계정 재발급 방식) 후, 운영자가 프로덕션 로그인 화면에서 직접 로그인해 **성공을 확인**함(세션 대화 기록: "로그인은 잘되는데 여전히 로그아웃버튼은 안나옴" — 로그인 성공 자체는 확인됨, 로그아웃 버튼 가시성 문제는 이후 별도로 발견·수정·배포 완료).

## 5. GO / NO-GO 판정

### **GO** (파일럿 1단계 — 운영자 단독 사용 개시 가능)

**근거**:
- §2.1이 요구하는 핵심 흐름(로그인 → 사건 제출 → 202 → 완료 → 리포트/피드백 저장·재조회)이 실제 프로덕션 DB 근거로 전부 PASS
- 스모크 데이터 정리 완료, 잔존 0건 재확인
- 테넌트 격리(§2.3)는 1단계 요구사항이 아님(문서 자체가 2단계 전제 조건으로 명시)
- Gemini 사용량(Premium 1회)은 하루 보수적 목표(15회) 대비 미미함

**잔여 위험(Residual-risk) — GO를 막지는 않으나 인지 필요**:
1. Netlify 배포 SHA가 `9050ff1`(최신 로그아웃 버튼 수정 포함)인지 대시보드로 직접 확인되지 않음 — 확인 전까지는 사이드바 로그아웃 버튼이 실제 사용자에게 아직 안 보일 수 있음(기능적으로 파일럿 핵심 흐름과는 무관).
2. Netlify 함수 로그 기반 429/5xx 확인은 이 세션에서 수행 불가 — DB 결과만으로는 간접 증거일 뿐, 문서가 요구하는 로그 레벨 확인은 아님. 운영 개시 직후 한 번 더 확인 권장.

## 교차 참조

- `.moai/docs/pilot-ops-launch-plan.md` §1~§2.2
- `.moai/docs/account-provisioning.md`
- `.moai/specs/SPEC-PILOT-OPS-001/spec.md`
