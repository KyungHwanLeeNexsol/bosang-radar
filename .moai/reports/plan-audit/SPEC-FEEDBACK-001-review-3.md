# SPEC Review Report: SPEC-FEEDBACK-001
Iteration: 3/3
Verdict: FAIL
Overall Score: 0.90

Reasoning context ignored per M1 Context Isolation. This is a full, fresh, skeptical re-audit against the CURRENT content of spec.md / plan.md / acceptance.md / progress.md — the prior 0.97 (iteration 2) verdict was NOT assumed to still hold.

## Must-Pass Results
- [PASS] MP-1 REQ number consistency: `spec.md` REQ-FEEDBACK-001..015 — 15 unique IDs, sequential, 3-digit zero-padding, no gaps/duplicates (grep verified: `REQ-FEEDBACK-[0-9]+` → 001,002,003,004,005,006,007,008,009,010,011,012,013,014,015, each appearing at its table row plus cross-reference mentions at spec.md:67-68,97,106).
- [PASS] MP-2 EARS/GEARS format compliance (REQ-XXX layer only — AC-XXX Given-When-Then entries in acceptance.md are the correct verification-layer format and are NOT modality-checked here, per M3 § Scope): every REQ-FEEDBACK-NNN row in spec.md §2 carries an explicit 유형 (type) column drawn from {Ubiquitous, Ubiquitous+Unwanted, Unwanted, When(이벤트 감지)} and each requirement body follows the corresponding GEARS shall/shall-not/when-shall pattern (e.g. spec.md:64 REQ-FEEDBACK-009 "When(이벤트 감지) | ... 감지되면(When-detected), 피드백 write-path 함수는 요청을 거부하고..."; spec.md:47 REQ-FEEDBACK-002 "Ubiquitous + Unwanted | ... 유일성 제약을 두어서는 안 되며 ... 제공해서는 안 된다"). No REQ entry mixes informal free-text or presents a Given/When/Then test scenario as a requirement.
- [PASS] MP-3 YAML frontmatter validity: spec.md:2-16 carries all 12 canonical fields with correct types verified against `.claude/rules/moai/development/spec-frontmatter-schema.md` — `id: SPEC-FEEDBACK-001` (matches regex), `version: "0.1.0"` (quoted semver), `status: draft` (valid enum), `created`/`updated`: `2026-09-01` (ISO), `priority: P1`, `phase: "v0.9.0 target"` (not a prohibited lifecycle-stage value), `module:` non-empty path list, `lifecycle: spec-anchored`, `tags:` comma-separated non-empty string. No snake_case aliases present. Optional fields `tier: M` and `depends_on: [...]` also present and well-formed.
- [N/A] MP-4 Section 22 language neutrality: SPEC is scoped to a single-language TypeScript/Next.js project (`lib/feedback/`, `lib/db/schema.ts`, `app/cases/[caseId]/`) — no multi-language tooling enumeration claim is made anywhere in spec.md/plan.md. Auto-pass per MP-4 precedent.
- [PASS] MP-5 D7 cross-SPEC reconciliation: `spec.md`/`plan.md` reference `SPEC-RESEARCH-001`, `SPEC-SCAFFOLD-001`, `SPEC-EVIDENCE-001` (grep-extracted, spec.md:15,20,114-116; plan.md:1). All three exist at `.moai/specs/<ID>/spec.md` and each carries `status: completed` (verified via `grep '^status:'` on each file). No status ∈ {retired, superseded, archived} — no BLOCKING finding.
- [N/A] MP-6 D8 cross-platform discipline: `grep -c syscall` on spec.md returns 0. D8 verification verb executed; literal string "syscall" does not appear anywhere in the SPEC body — auto-PASS per D8-4 (no cross-platform discipline concern for this pure TypeScript/web-app SPEC).
- [PASS] MP-7 clarification gate: `grep -rn '\[NEEDS CLARIFICATION' .moai/specs/SPEC-FEEDBACK-001/plan.md` (and directory-wide) returns no matches. `plan.md` exists (Tier M has no `research.md`, so this is the applicable artifact) and carries zero unresolved `[NEEDS CLARIFICATION: ...]` markers — this is a genuine PASS, not N/A (plan.md exists and was checked).

**All seven Must-Pass criteria clear.** The FAIL verdict below is driven by an independently-verified, out-of-band integrity defect discovered during adversarial re-verification of the "7 latest corrections" — see D7 in Defects Found. This is not an M5 firewall failure in the strict MP-1..MP-7 sense, but it is a genuine, concrete, falsifiable defect surfaced by direct evidence, and per this audit's explicit task directive ("If you find a genuine blocking defect, report it clearly as FAIL... do not soften a real blocker just because this is the final iteration"), it drives the overall verdict.

## Category Scores (0.0-1.0, rubric-anchored)
| Dimension | Score | Rubric Band | Evidence |
|-----------|-------|-------------|----------|
| Clarity | 1.0 | 1.0 (every requirement single unambiguous interpretation) | spec.md:53-58 (REQ-003~007) spell out empty-string→omitted normalization and all-or-nothing `outcome` semantics to the character level, with explicit non-examples (`"abc"`, `"2026-99-99"`); spec.md:64-68 (REQ-009~013) fully specify tie-break ordering, bounds semantics, and existence-only permissiveness with explicit UI-vs-write-path scope carve-outs. No REQ requires interpretive judgment. |
| Completeness | 0.90 | 0.75→ (one artifact carries a materially inaccurate self-report — see D7) | HISTORY/WHY/WHAT/REQUIREMENTS/§3 AC pointer/§4 cross-refs all present in spec.md; `### Out of Scope — <topic>` H3 sub-headings ×6 each with `-` bullets (spec.md:79-106); frontmatter complete (12/12 fields). Deducted from 1.0 because `progress.md` §E.1 (an artifact within this audit's scope) makes a specific, checkable claim — "force-committed alongside this amendment... so this citation is verifiable from the branch" (progress.md:33) — that is independently verified FALSE (see D7 Evidence). A SPEC's audit-trail record is part of its completeness; a false record is an incomplete/inaccurate one. |
| Testability | 1.0 | 1.0 (every AC binary-testable, no weasel words) | All 16 ACs in acceptance.md use Given-When-Then with concrete literal values (`"2026-99-99"`, `claimIndex: 5`, `{overallRating: "ACCURATE"}`) and objective PASS conditions (`result.success === true`, exact row counts, exact `data-testid` counts). No instance of "appropriate"/"adequate"/"reasonable"/"good"/"proper" found in any AC body. |
| Traceability | 1.0 | 1.0 (every REQ has ≥1 AC, every AC references a valid REQ, no orphans) | REQ-001→AC Group 1 (001-002); REQ-002→AC Group 3 heading + AC-013 negative-half; REQ-003~008→AC Group 2 (003-008); REQ-009~013→AC Group 3 (009-013); REQ-014/015→AC Group 4 (014-015); AC-016 is a deliverable-level (not per-REQ) test-suite-green criterion, valid at Tier M. No REQ uncovered, no AC referencing a non-existent REQ. |

## Verification of the 7 Requested Corrections (all independently re-checked against current file content)

1. **Test-scope completeness** — CONFIRMED landed. `plan.md` M5 (line 67) adds `scripts/provision-tester.test.ts` as an edit target: `toHaveLength(4)` → `toHaveLength(5)`, a new `FEEDBACK_MIGRATION` constant, and a table-scope drift-guard. Independently re-verified the pre-amendment baseline this reacts to: `scripts/provision-tester.test.ts:271` currently reads `expect(sqlFiles).toHaveLength(4)` against 4 existing migration files (`db/migrations/0000_*.sql`..`0003_*.sql`, confirmed via Glob) — the plan's premise is factually accurate. `acceptance.md`'s Definition of Done (line 112) explicitly names both `scripts/provision-tester.test.ts` and `lib/cases/get-case-for-owner.test.ts` as recognized in-scope M5 edits.
2. **Test responsibility split** — CONFIRMED landed and coherent. `plan.md` M5 (line 64) adds `lib/cases/get-case-for-owner.test.ts` as an edit target (`orderBy` added to the mocked chain; multi-report determinism scenarios relocated here). Independently re-verified the current mocked chain in `lib/cases/get-case-for-owner.test.ts` — it stubs only `select/from/where/limit` with **no** `orderBy` link, confirming the plan's stated mock-chain-mismatch premise is real and the fix is necessary. `plan.md` M5's `submit-feedback.test.ts` bullet (line 63) is correspondingly narrowed ("this test does NOT re-test report-selection determinism ... report-selection determinism is covered exclusively by `lib/cases/get-case-for-owner.test.ts` below"). No scenario is orphaned or duplicated: AC-FEEDBACK-010's ordering half maps 1:1 to the relocated `get-case-for-owner.test.ts` scenarios (line 64: "the returned `report` object and the returned `reportId` field come from the exact same selected row"). One minor residual gap noted as an optional finding below (D8).
3. **progress.md honesty** — PARTIALLY landed; one half is FALSE (see D7, the driver of this FAIL verdict). The top-level `plan_audit_verdict` line (progress.md:12) correctly shows the canonical current state ("PASS (score 0.97, iteration 2/3)") with the 0.92 round preserved as explicit sub-bullet history (progress.md:13) — this half is confirmed correct. The second half — the claim that `review-2.md` is "force-committed alongside this amendment... so this citation is verifiable from the branch" (progress.md:33) — was independently verified via `git ls-files`, `git log --all --diff-filter=A --name-only`, and `git check-ignore -v`, and is **false**: the file is untracked, appears in zero commits across all branches, and is still actively matched by `.gitignore:210`. See D7.
4. **SSOT reference cleanup** — CONFIRMED landed. Both remaining mentions of `lib/feedback/types.ts` in `plan.md` (line 7: "no separate hand-written `lib/feedback/types.ts` file exists anywhere in this plan"; line 20: "No `lib/feedback/types.ts` hand-written interface file is created") are unambiguous negations. Zero mentions claim this file as the SSOT location.
5. **AC-FEEDBACK-013 completeness** — CONFIRMED landed. Both success-path assertions in `acceptance.md` lines 83 and 85 now require the full `{ success: true, feedbackId: <id>, caseId: <id> }` shape (line 83 explicitly parenthetical: "not just `{success:true, feedbackId}`"), and line 85 adds the minimal-payload (`{ overallRating: "ACCURATE" }` only) insert-success scenario as required.
6. **Empty-string / outcome all-or-nothing contract** — CONFIRMED landed on both requirement and acceptance-criterion sides. `spec.md` REQ-FEEDBACK-003 (line 53), REQ-FEEDBACK-004 (line 54), REQ-FEEDBACK-005 (line 55) each state the trim→empty→omitted (never `""`) normalization explicitly. REQ-FEEDBACK-007 (line 57) states both halves of the all-or-nothing contract (fully-empty → omitted entirely; exactly-one-filled → schema validation failure). `acceptance.md` AC-FEEDBACK-004 (lines 31-32) carries both the empty-string-normalization scenario and the partial-outcome-rejection scenario as required.
7. **Empty-array query-safety contract** — CONFIRMED landed. `plan.md` M3 (line 43, "implementation contract (empty-array query safety)") explicitly states the step-3/step-4 existence-check queries MUST be skipped entirely for empty `claimAssessments`/`evidenceAssessments`, with an explicit prohibition on issuing an empty `IN ()` clause.

## Defects Found (structured defect-list)

D7. **integrity-false-claim** — `progress.md:33` — The claim "(force-committed alongside this amendment, overriding the normal `.gitignore` exclusion for plan-audit reports ... specifically so this citation is verifiable from the branch; prior to this force-add the file existed on disk but was NOT committed or reachable from the branch)" is factually false as of this audit. Independently verified via three separate commands: `git ls-files | grep review-2` → no output (untracked); `git log --all --diff-filter=A --name-only -- .moai/reports/plan-audit/SPEC-FEEDBACK-001-review-2.md` → zero commits across all branches ever added this file; `git check-ignore -v .moai/reports/plan-audit/SPEC-FEEDBACK-001-review-2.md` → `.gitignore:210:.moai/reports/plan-audit/*.md` (still actively ignored, never force-added to the index). The cited report is NOT reachable from the branch, contradicting the explicit written claim. This is precisely the class of defect `verification-claim-integrity.md` §1.1 surface 1 exists to prevent (an unobserved/false completion claim) — the round-3 "fix" changed the wording to assert a git operation that was never actually performed. — Severity: critical — Class: blocking — Required fix: EITHER (a) actually run `git add -f .moai/reports/plan-audit/SPEC-FEEDBACK-001-review-2.md` and commit it so the citation becomes true, OR (b) correct `progress.md:33` to honestly state the file is NOT committed/reachable from the branch (revert to the pre-round-3 accurate framing) — pick one and make the prose match the actual git state. This is a one-line or one-command fix; it does not require re-touching spec.md/plan.md/acceptance.md.

D8. **AC-write-path residual gap (optional)** — `acceptance.md:67` / `plan.md:64` — AC-FEEDBACK-010's "And (multi-report determinism...)" clause literally states "...a feedback submission using that returned `reportId` inserts a row whose `reportId` equals `report-2`'s id." The relocated test ownership (M5, `lib/cases/get-case-for-owner.test.ts`) tests only the `getCaseForOwner` ordering/tie-break half, not an actual `submitReportFeedback` call chained after it. In practice this is low-risk (the write-path treats `reportId` as an opaque pass-through parameter, already covered by other `submit-feedback.test.ts` cases), so this is not a defect in the requirement/AC text itself — merely a literal AC clause with no single test file that exercises it end-to-end as written. — Severity: minor — Class: optional — Required fix (if the orchestrator wants full literal coverage): note in `plan.md` M5 that this composed scenario is covered by the combination of `get-case-for-owner.test.ts` (ordering) + `submit-feedback.test.ts`'s existing "valid submission inserts...with `caseId` derived from the mocked report/case join" case (persistence), or add one integration-style test explicitly chaining both.

D9. **AC-FEEDBACK-013 density (optional, readability)** — `acceptance.md:80-85` — This single AC ID now bundles three distinct scenarios under one Given/When/Then + two "And" clauses (append-only double-insert, REQ-002 negative-half static-inspection, minimal-payload insert-success), grown across two amendment rounds. Each sub-scenario is individually clear and correctly labeled with its governing REQ, so this is not a blocking clarity defect, but a reader scanning AC-FEEDBACK-013 top-to-bottom now processes three independent claims under one heading. — Severity: minor — Class: optional — Required fix (if desired): none required for PASS; optionally split into AC-FEEDBACK-013a/013b/013c in a future revision, understanding the SPEC's Tier M AC ceiling (16) is not yet exceeded so there is room to split without a tier change.

### Carried-over findings from iteration 2 (regression check against prior report)

- D2 (carried over, **partially resolved**): The original finding was "no negative-path AC for enum/partial-outcome rejection." The partial-outcome half is now RESOLVED — AC-FEEDBACK-004 (line 32) adds the partial-`outcome`-rejection scenario as part of this round's fix #6. The enum half remains unresolved: no AC anywhere tests an invalid `overallRating` value being rejected (grep for an invalid-`overallRating` negative scenario in acceptance.md returns no matches). — Severity: minor — Class: optional (per orchestrator's original deferral decision, unchanged).
- D3 (carried over, unresolved, unchanged): `spec.md` REQ-FEEDBACK-012/013 (lines 67-68) still carry only the `Ubiquitous`/`When(이벤트 감지)` label without the `+ Unwanted` compound suffix, despite both containing "shall not"-shaped clauses in their body text. — Severity: minor — Class: optional (cosmetic taxonomy nit; the REQ body text itself is unambiguous regardless of the label).
- D4 (carried over, unresolved, unchanged): `plan.md:36` still reads "extend the `reports` select to also project `id: reports.id`" — imprecise phrasing (Drizzle terminology quibble), does not affect implementability. — Severity: minor — Class: optional.
- D5 (carried over, unresolved, unchanged): `spec.md:38` Tier-M rationale still cites "약 9-10개 파일" while `plan.md` §B enumerates roughly 13-14 discrete milestone bullet items across 5 milestones. Does not change the Tier M classification (still well within the 5-15 file Tier M band per `spec-workflow.md` § SPEC Complexity Tier). — Severity: minor — Class: optional.
- D6 (carried over, unresolved, unchanged): REQ-FEEDBACK-009 (spec.md:64) still bundles two distinct behaviors (ownership-rejection + deterministic report selection) under one ID. Both halves are independently covered by ACs (AC-FEEDBACK-009 for ownership; AC-FEEDBACK-010's "And" clause for determinism), so traceability is intact; this is a requirements-granularity style preference, not a defect. — Severity: minor — Class: optional.

No must-pass regression. No previously-fixed item was un-fixed in this round, except that D7 above represents a *new* false claim introduced by this round's attempt to fix the progress.md honesty issue (item #3) — the SPEC took one step forward (accurate top-level verdict) and one step sideways (a still-inaccurate reachability claim, now differently worded but equally false).

## Regression Check (Iteration 2 → 3)
Defects from iteration 2 (`SPEC-FEEDBACK-001-review-2.md`):
- D2 (enum/partial-outcome AC gap): partial-outcome half — **RESOLVED** (AC-FEEDBACK-004 line 32). Enum half — **UNRESOLVED** (unchanged, still optional/deferred).
- D3 (REQ-012/013 compound label): **UNRESOLVED** (unchanged, optional/deferred).
- D4 (M3 "extend the select" wording): **UNRESOLVED** (unchanged, optional/deferred).
- D5 (Tier-M file-count estimate): **UNRESOLVED** (unchanged, optional/deferred).
- D6 (REQ-009 bundled behaviors): **UNRESOLVED** (unchanged, optional/deferred).

No defect from iteration 2 appears in all three iterations unchanged in a way that indicates stagnation on a REQUIRED item — D2-D6 were explicitly classified as optional/discretionary by the orchestrator in iteration 2 and remain so; their non-resolution is expected, not a "blocking defect — manager-spec made no progress" signal.

## Recommendation

**This is the final iteration (3/3) under the Retry Loop Contract.** The FAIL verdict is driven by exactly one concrete, verified defect (D7) which is trivial to resolve and does not require touching spec.md, plan.md, or acceptance.md at all:

1. **Required fix for D7** (pick one):
   - (a) Run `git add -f .moai/reports/plan-audit/SPEC-FEEDBACK-001-review-2.md .moai/reports/plan-audit/SPEC-FEEDBACK-001-review-1.md` and commit, so the "verifiable from the branch" claim in `progress.md:33` becomes true, OR
   - (b) Edit `progress.md:33` to remove the false "force-committed... verifiable from the branch" claim and state accurately that the report exists on disk only (matching the honest framing progress.md itself used before this round's edit).
2. No other required fixes. All 7 requested corrections (items 1,2,4,5,6,7) are independently confirmed correctly landed with cross-file evidence; item 3 is half-correct (see above).
3. D8/D9 and the carried-over D2-D6 remain optional/discretionary per the established M6 finding-consumption discipline — do not let them block a re-confirmation pass.

**Per the LEAN Workflow iteration-cap escalation** (this is iteration 3, the hard cap): given the sole blocking defect is a one-line/one-command fix unrelated to the SPEC's technical content, the recommended path is a **trivial in-place fix + a scoped confirming re-check of D7 only** (not a from-scratch iteration 4, which the retry-loop contract does not permit without explicit user override). Present the user with:
   - **Option 1 (recommended)**: Apply fix (a) or (b) above, then accept the SPEC as PASS-with-debt against the fixed content (the fix itself needs no further plan-auditor re-run since it touches only progress.md's audit-trail prose/git-tracking, not the technical spec/plan/acceptance content that iterations 1-3 have already validated at 0.90-0.97).
   - **Option 2**: Explicit user override to spend a 4th plan-auditor pass anyway.
   - **Option 3**: Accept current state as PASS-with-debt without fixing D7, explicitly documenting the known-false audit-trail claim as accepted residual debt.

If the user selects Option 1 and applies fix (a) or (b), this SPEC's plan-phase artifacts are ready for run-phase entry — the underlying spec.md/plan.md/acceptance.md content is of very high quality (Clarity 1.0, Completeness 1.0-on-the-SPEC-content-itself, Testability 1.0, Traceability 1.0) and all three prior audit rounds' substantive technical findings have been correctly and verifiably addressed.
